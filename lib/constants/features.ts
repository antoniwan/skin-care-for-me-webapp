/**
 * Product lookup (add by name) is stubbed — mock/AI flow not ready for users.
 * Flip to `true` when lookup is production-ready and OPENAI_API_KEY is configured.
 */
export const PRODUCT_LOOKUP_ENABLED = false;
